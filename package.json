{
  "name": "anilist-proxy", 
  "version": "1.0.0",
  "description": "A proxy server for Anilist GraphQL API",
  "main": "api/graphql.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "cross-fetch": "^4.1.0"
  },
  "devDependencies": {
    "vercel": "^32.3.0"
  }
}
